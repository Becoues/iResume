services:
  iresume:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - ./prisma/dev.db:/app/data/dev.db
    environment:
      - DATABASE_URL=file:/app/data/dev.db
    restart: unless-stopped
