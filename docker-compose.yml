services:
  iresume:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - iresume-data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/dev.db
    restart: unless-stopped

volumes:
  iresume-data:
